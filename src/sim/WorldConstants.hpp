#pragma once

namespace evolab {

inline constexpr float kWorldCellSize = 1.2f;
inline constexpr float kTerrainHeightScale = 0.15f;
// Visual / HUD day-night period (matches VisualApp DayCycle).
inline constexpr float kVisualDayCyclePeriodTicks = 1800.0f;

}  // namespace evolab
